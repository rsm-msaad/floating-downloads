import SwiftUI

/// Placeholder entry point. This exists only so the target compiles during
/// project setup (phase 1a). It is replaced entirely in the next phase by the
/// real MenuBarExtra + floating NSPanel implementation.
@main
struct FloatingDownloadsApp: App {
    var body: some Scene {
        WindowGroup {
            Text("placeholder")
        }
    }
}
